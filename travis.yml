language: python # => 1
    python: # => 2
      - "3.6"
      - "3.7"
    services: # => 3
      - mysql
    env: # => 4
      -DJANGO=2.1 DB=postgres
    install: # => 5
      - pip install -r requirements.txt
    before_script: # => 6
      - postgres -e 'create database django_auth;' -u root
    script: # => 7
      - python manage.py test